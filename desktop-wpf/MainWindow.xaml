<Window x:Class="SecurityMindset.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SecurityMindset - Login" Height="420" Width="560">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Đăng nhập" FontSize="24" FontWeight="Bold"/>
        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="Username"/>
            <TextBox x:Name="UsernameBox" MinWidth="320"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock Text="Password"/>
            <PasswordBox x:Name="PasswordBox" MinWidth="320"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,16,0,0">
            <Button Content="Đăng nhập" Click="OnLoginClick" Padding="12,6"/>
            <TextBlock x:Name="ResultText" Margin="12,6,0,0"/>
        </StackPanel>
    </Grid>
</Window>


